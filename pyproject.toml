[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gfg-store"
version = "0.1.0"
description = "Starter Python package for GFG-Store"
authors = [{name = "OrbitCoder-Store"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]